<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.oldmerman.web.mapper.ArticleLikeRecordMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO o_blog.o_article_like_record (user_id, article_id)
        VALUES (#{userId}, #{articleId})
    </insert>
</mapper>

